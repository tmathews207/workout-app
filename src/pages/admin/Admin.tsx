import { PageShell } from '../../components/PageShell'

// TODO: manage `modalities` (add/edit), edit `rating_descriptions` per
// scale_key/rating, and browse/edit/delete past entries across sleep_logs,
// weight_logs, readiness_logs, and sessions.
export default function Admin() {
  return (
    <PageShell title="Admin" description="Modalities, rating descriptions, and past entries.">
      <p className="text-sm text-slate-400">Not yet implemented — see TODO in this file.</p>
    </PageShell>
  )
}
