import { PageShell } from '../../components/PageShell'

// TODO: pick a date, create/edit a `sessions` row plus its three
// `session_phases` (preparatory/training/recovery), add `session_activities`
// from the library, and enter `planned_sets` per activity. Primarily used
// from the laptop.
export default function PlanSession() {
  return (
    <PageShell title="Plan a session" description="Preparatory, training, and recovery phases for a given date.">
      <p className="text-sm text-slate-400">Not yet implemented — see TODO in this file.</p>
    </PageShell>
  )
}
